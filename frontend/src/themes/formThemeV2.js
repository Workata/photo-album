import { createTheme } from '@mui/material/styles';

const formThemeV2 = createTheme({
  palette: {
    primary: {
      main: '#ffffff',
    },
    secondary: {
      main: '#000000',
    },
  },
});

export default formThemeV2;